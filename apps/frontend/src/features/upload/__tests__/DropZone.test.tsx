import { describe, it } from "vitest";

describe("DropZone", () => {
  it.todo("extension: rejects .xlsx file with inline error message");
  it.todo("extension: accepts .csv file and calls onChange");
  it.todo("extension: accepts .txt file and calls onChange");
  it.todo("size: rejects file >10MB with inline error message");
  it.todo("multi-drop: uses first file and shows warning when multiple files dropped");
  it.todo("click: opens file picker on button click");
});
