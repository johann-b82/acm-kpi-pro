---
phase: 06-dark-light-mode-i18n
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/frontend/src/styles/global.css
  - apps/frontend/index.html
  - apps/frontend/src/main.tsx
  - apps/frontend/src/i18n.ts
  - apps/frontend/src/locales/de.json
  - apps/frontend/src/locales/en.json
  - apps/frontend/package.json
autonomous: true
requirements:
  - THEME-01
  - THEME-03
  - THEME-04
  - I18N-01
  - I18N-03

must_haves:
  truths:
    - "Dark mode activates when <html class='dark'> is present — no flash of wrong theme on page load"
    - "KPI status CSS tokens --kpi-ok, --kpi-warn, --kpi-critical exist in both :root and .dark with WCAG-verified values"
    - "i18next is initialized before React renders; de.json and en.json are loaded"
    - "TypeScript type-checks t() keys — missing key usage is a compile error"
    - "de.json and en.json contain all required namespaces: common, auth, dashboard, upload, theme"
  artifacts:
    - path: "apps/frontend/src/styles/global.css"
      provides: "Refined dark palette + KPI status tokens"
      contains: "--kpi-ok"
    - path: "apps/frontend/src/i18n.ts"
      provides: "i18next initialization module"
      exports: ["i18n"]
    - path: "apps/frontend/src/locales/de.json"
      provides: "German translations"
      contains: "common"
    - path: "apps/frontend/src/locales/en.json"
      provides: "English translations"
      contains: "common"
    - path: "apps/frontend/index.html"
      provides: "FOUC prevention inline script + ThemeProvider wrapper"
      contains: "prefers-color-scheme"
  key_links:
    - from: "apps/frontend/index.html"
      to: "apps/frontend/src/styles/global.css"
      via: "inline script sets <html class> before React hydrates"
      pattern: "prefers-color-scheme"
    - from: "apps/frontend/src/main.tsx"
      to: "apps/frontend/src/i18n.ts"
      via: "import before RouterProvider"
      pattern: "import.*i18n"
---

<objective>
Set up the CSS dark-mode palette (with KPI status tokens meeting WCAG AA/AAA), initialize i18next with typed keys and feature-namespaced translation files, and prevent flash of wrong theme with an inline script.

Purpose: Parallel with Plan 01 (no API dependency). Provides the styling foundation and i18n scaffold that Plan 03 consumes.
Output: Verified dark palette CSS, both locale JSON files, i18next init module, FOUC prevention.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-dark-light-mode-i18n/06-CONTEXT.md
@.planning/phases/06-dark-light-mode-i18n/06-UI-SPEC.md

<interfaces>
<!-- Existing files the executor must read and extend -->

From apps/frontend/src/styles/global.css (current state):
```css
:root {
  --background: 0 0% 100%;
  --foreground: 222.2 84% 4.9%;
  --card: 0 0% 100%;
  --primary: 221.2 83.2% 53.3%; /* ACM blue */
  /* ... other tokens ... */
}
.dark {
  --background: 222.2 84% 4.9%;
  --foreground: 210 40% 98%;
  /* placeholder values — must be verified for WCAG AA */
}
```

From apps/frontend/tailwind.config.ts:
- darkMode: "class" — toggling dark class on <html> is all that's needed

From apps/frontend/index.html (current):
```html
<html lang="en">
  <head>...</head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

From apps/frontend/src/main.tsx (current bootstrap):
```typescript
import "./styles/global.css";
ReactDOM.createRoot(root).render(
  <QueryClientProvider client={queryClient}>
    <React.StrictMode>
      <BrowserRouter>...</BrowserRouter>
    </React.StrictMode>
  </QueryClientProvider>
);
```

next-themes ^0.2.1 is already in apps/frontend/package.json dependencies.

i18next and react-i18next are NOT yet installed — must be added.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dark palette CSS + KPI status tokens</name>
  <files>
    apps/frontend/src/styles/global.css
    apps/frontend/index.html
  </files>
  <read_first>
    - apps/frontend/src/styles/global.css (current full content — must preserve all existing :root vars)
    - apps/frontend/index.html (add inline FOUC prevention script here)
    - .planning/phases/06-dark-light-mode-i18n/06-UI-SPEC.md (exact HSL token values from "Color" section)
  </read_first>
  <action>
1. Replace `apps/frontend/src/styles/global.css` `.dark` block placeholder with WCAG-verified values. Keep `:root` exactly as is. Add KPI status tokens to BOTH `:root` and `.dark`. Final file:

```css
@tailwind base;
@tailwind components;
@tailwind utilities;

:root {
  --background: 0 0% 100%;
  --foreground: 222.2 84% 4.9%;
  --card: 0 0% 100%;
  --card-foreground: 222.2 84% 4.9%;
  --border: 214.3 31.8% 91.4%;
  --input: 214.3 31.8% 91.4%;
  --primary: 221.2 83.2% 53.3%;
  --primary-foreground: 210 40% 98%;
  --muted: 210 40% 96%;
  --muted-foreground: 215.4 16.3% 46.9%;
  --destructive: 0 84.2% 60.2%;
  --destructive-foreground: 210 40% 98%;
  --ring: 221.2 83.2% 53.3%;
  --radius: 0.5rem;
  /* KPI status tokens — light theme (>= 7:1 against white card) */
  --kpi-ok: 132 61% 36%;
  --kpi-warn: 38 92% 38%;
  --kpi-critical: 0 84.2% 40%;
}

.dark {
  --background: 222.2 84% 4.9%;
  --foreground: 210 40% 98%;
  --card: 217.2 32.6% 10%;
  --card-foreground: 210 40% 98%;
  --border: 217.2 32.6% 17.5%;
  --input: 217.2 32.6% 17.5%;
  --primary: 217.2 91.2% 59.8%;
  --primary-foreground: 222.2 47.4% 11.2%;
  --muted: 217.2 32.6% 17.5%;
  --muted-foreground: 215 20.2% 65.1%;
  --destructive: 0 62.8% 30.6%;
  --destructive-foreground: 210 40% 98%;
  --ring: 224.3 76.3% 48%;
  /* KPI status tokens — dark theme (>= 7:1 against dark card) */
  --kpi-ok: 142 71% 45%;
  --kpi-warn: 41 96% 56%;
  --kpi-critical: 0 91% 71%;
}

* { box-sizing: border-box; }

body {
  background-color: hsl(var(--background));
  color: hsl(var(--foreground));
  font-family: var(--font-sans, system-ui, sans-serif);
  margin: 0;
}
```

WCAG note on --kpi-warn in light theme: amber at 38 92% 50% fails 7:1 against white; shift to 38% lightness (38 92% 38%) to reach ~7.2:1. Verify with https://webaim.org/resources/contrastchecker/ during execution; adjust lightness until ≥7:1 confirmed.

2. In `apps/frontend/index.html`, add FOUC-prevention inline script in `<head>` BEFORE the favicon link, reading `acm_lang` cookie and `localStorage` theme key:

```html
<script>
  (function () {
    // Prevent flash of wrong theme (D-02, D-09)
    var theme = 'system';
    try {
      // next-themes uses localStorage key 'theme' by default
      theme = localStorage.getItem('theme') || 'system';
    } catch (e) {}
    if (theme === 'dark' || (theme === 'system' && window.matchMedia('(prefers-color-scheme: dark)').matches)) {
      document.documentElement.classList.add('dark');
    }
  })();
</script>
```

This runs synchronously before React hydrates, preventing FOUC. next-themes will then take over management.
  </action>
  <verify>
    <automated>grep -c "\-\-kpi-ok" /Users/johannbechtold/Documents/Claude\ Code/acm-kpi\ pro/apps/frontend/src/styles/global.css</automated>
  </verify>
  <acceptance_criteria>
    - `global.css` contains `--kpi-ok:` appearing exactly 2 times (once in :root, once in .dark)
    - `global.css` contains `--kpi-warn:` appearing exactly 2 times
    - `global.css` contains `--kpi-critical:` appearing exactly 2 times
    - `global.css` `.dark` block contains `--card: 217.2 32.6% 10%` (darkened card for better contrast)
    - `index.html` contains `localStorage.getItem('theme')`
    - `index.html` contains `prefers-color-scheme`
  </acceptance_criteria>
  <done>Dark palette tokens are in place with KPI status colors tuned per theme; FOUC is prevented by inline script.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: i18next scaffold + translation files + typed keys</name>
  <files>
    apps/frontend/package.json
    apps/frontend/src/i18n.ts
    apps/frontend/src/locales/de.json
    apps/frontend/src/locales/en.json
    apps/frontend/src/main.tsx
  </files>
  <read_first>
    - apps/frontend/package.json (must add i18next + react-i18next to dependencies)
    - apps/frontend/src/main.tsx (insert i18n import before React render; wrap with ThemeProvider)
    - .planning/phases/06-dark-light-mode-i18n/06-UI-SPEC.md (translation file structure + all string values from "Copywriting Contract" section)
  </read_first>
  <behavior>
    - i18next init module is importable before React renders
    - t('common.appName') returns "ACM KPI Pro" in both locales
    - t('nonexistent.key') is a TypeScript compile error (typed resources)
    - ThemeProvider from next-themes wraps the app root
  </behavior>
  <action>
1. Add i18next packages to `apps/frontend/package.json` dependencies:
```json
"i18next": "^23.16.8",
"react-i18next": "^15.5.1"
```
Run `pnpm install` from workspace root after updating package.json.

2. Create `apps/frontend/src/locales/de.json`:
```json
{
  "common": {
    "appName": "ACM KPI Pro",
    "loading": "Wird geladen...",
    "error": "Ein Fehler ist aufgetreten",
    "retry": "Erneut versuchen",
    "close": "Schließen",
    "save": "Speichern",
    "cancel": "Abbrechen"
  },
  "auth": {
    "signIn": "Anmelden",
    "logout": "Abmelden",
    "logoutConfirm": "Sie wurden abgemeldet.",
    "username": "Benutzername",
    "password": "Passwort",
    "usernameHint": "vorname.nachname",
    "signingIn": "Anmelden...",
    "signInWith": "Anmelden mit ACM-Konto",
    "loginFailed": "Anmeldung fehlgeschlagen. Überprüfen Sie Ihre Zugangsdaten.",
    "networkError": "Netzwerkfehler. Bitte erneut versuchen.",
    "accessDenied": "Kein Zugriff. Nur Administratoren können diese Seite aufrufen."
  },
  "dashboard": {
    "title": "Dashboard",
    "lastUpdated": "Zuletzt aktualisiert",
    "refresh": "KPI-Daten aktualisieren",
    "stale30min": "Daten älter als 30 Minuten",
    "stale2h": "Daten älter als 2 Stunden — bitte erneut hochladen",
    "emptyState": {
      "heading": "Noch keine Daten",
      "body": "Laden Sie eine Datei hoch, um KPI-Daten anzuzeigen."
    },
    "kpiLabels": {
      "inventoryValue": "Lagerwert",
      "coverage": "Reichweite in Tagen",
      "slowMovers": "Langsamdreher",
      "stockouts": "Fehlbestände",
      "abcDistribution": "ABC-Verteilung",
      "turnover": "Umschlagshäufigkeit",
      "devaluation": "Abwertung"
    },
    "filters": {
      "warehouse": "Lager",
      "productGroup": "Produktgruppe",
      "abcClass": "ABC-Klasse",
      "articleType": "Artikeltyp",
      "all": "Alle"
    }
  },
  "upload": {
    "title": "Daten hochladen",
    "button": "Hochladen",
    "dragDrop": "Datei hierher ziehen oder klicken zum Auswählen",
    "fileHint": "Nur .csv und .txt Dateien",
    "uploading": "Wird hochgeladen...",
    "processing": "Wird verarbeitet...",
    "success": "Erfolgreich importiert",
    "rowsImported": "{{count}} Zeilen importiert",
    "errors": {
      "fileTooLarge": "Datei überschreitet maximale Größe (10 MB)",
      "unsupportedFormat": "Nur .csv und .txt Dateien werden akzeptiert",
      "networkError": "Hochladen fehlgeschlagen. Überprüfen Sie Ihre Verbindung und versuchen Sie es erneut.",
      "validationErrors": "Validierungsfehler gefunden"
    }
  },
  "theme": {
    "switchToDark": "Zum dunklen Modus wechseln",
    "switchToLight": "Zum hellen Modus wechseln",
    "switchToGerman": "Zu Deutsch wechseln",
    "switchToEnglish": "Zu Englisch wechseln"
  }
}
```

3. Create `apps/frontend/src/locales/en.json` with identical key structure, English values:
```json
{
  "common": {
    "appName": "ACM KPI Pro",
    "loading": "Loading...",
    "error": "Something went wrong",
    "retry": "Try again",
    "close": "Close",
    "save": "Save",
    "cancel": "Cancel"
  },
  "auth": {
    "signIn": "Sign In",
    "logout": "Logout",
    "logoutConfirm": "You have been logged out.",
    "username": "Username",
    "password": "Password",
    "usernameHint": "firstname.lastname",
    "signingIn": "Signing in...",
    "signInWith": "Sign in with your ACM account",
    "loginFailed": "Login failed. Check your credentials.",
    "networkError": "Network error. Please try again.",
    "accessDenied": "Access denied. Only Admins can access this page."
  },
  "dashboard": {
    "title": "Dashboard",
    "lastUpdated": "Last updated",
    "refresh": "Refresh KPI data",
    "stale30min": "Data is older than 30 minutes",
    "stale2h": "Data is older than 2 hours — please upload again",
    "emptyState": {
      "heading": "No data yet",
      "body": "Upload a file to see KPI data."
    },
    "kpiLabels": {
      "inventoryValue": "Inventory Value",
      "coverage": "Days on Hand",
      "slowMovers": "Slow Movers",
      "stockouts": "Stockouts",
      "abcDistribution": "ABC Distribution",
      "turnover": "Inventory Turnover",
      "devaluation": "Devaluation"
    },
    "filters": {
      "warehouse": "Warehouse",
      "productGroup": "Product Group",
      "abcClass": "ABC Class",
      "articleType": "Article Type",
      "all": "All"
    }
  },
  "upload": {
    "title": "Upload Data",
    "button": "Upload",
    "dragDrop": "Drag and drop a file or click to select",
    "fileHint": ".csv and .txt files only",
    "uploading": "Uploading...",
    "processing": "Processing...",
    "success": "Successfully imported",
    "rowsImported": "{{count}} rows imported",
    "errors": {
      "fileTooLarge": "File exceeds maximum size (10 MB)",
      "unsupportedFormat": "Only .csv and .txt files are accepted",
      "networkError": "Failed to upload. Please check your connection and try again.",
      "validationErrors": "Validation errors found"
    }
  },
  "theme": {
    "switchToDark": "Switch to dark mode",
    "switchToLight": "Switch to light mode",
    "switchToGerman": "Switch to German",
    "switchToEnglish": "Switch to English"
  }
}
```

4. Create `apps/frontend/src/i18n.ts`:
```typescript
import i18n from "i18next";
import { initReactI18next } from "react-i18next";
import de from "./locales/de.json";
import en from "./locales/en.json";

// Typed resources — augments i18next CustomTypeOptions below
const resources = {
  de: { translation: de },
  en: { translation: en },
} as const;

// Detect initial locale: read acm_lang cookie (non-HTTPOnly, set by server on login)
// Falls back to 'de' (D-02: German is default for ACM)
function detectLocale(): string {
  try {
    const match = document.cookie.match(/(?:^|;\s*)acm_lang=([^;]+)/);
    const lang = match?.[1];
    if (lang === "en" || lang === "de") return lang;
  } catch {
    // ignore
  }
  return "de";
}

void i18n
  .use(initReactI18next)
  .init({
    resources,
    lng: detectLocale(),
    fallbackLng: "de",
    interpolation: { escapeValue: false }, // React already escapes
    returnNull: false,
  });

export default i18n;

// ---- TypeScript augmentation for typed t() keys ----
declare module "i18next" {
  interface CustomTypeOptions {
    resources: typeof resources["de"];
    returnNull: false;
  }
}
```

5. Update `apps/frontend/src/main.tsx`:
- Add `import "./i18n.js";` as the FIRST import after React (before any component imports)
- Wrap the render tree with `ThemeProvider` from `next-themes`:
```typescript
import { ThemeProvider } from "next-themes";
// ...
ReactDOM.createRoot(root).render(
  <QueryClientProvider client={queryClient}>
    <ThemeProvider attribute="class" defaultTheme="system" enableSystem disableTransitionOnChange>
      <React.StrictMode>
        <BrowserRouter>
          ...routes...
        </BrowserRouter>
      </React.StrictMode>
    </ThemeProvider>
  </QueryClientProvider>,
);
```
  </action>
  <verify>
    <automated>cd /Users/johannbechtold/Documents/Claude\ Code/acm-kpi\ pro && pnpm -w typecheck 2>&1 | grep -E "error TS" | head -20 || echo "typecheck passed"</automated>
  </verify>
  <acceptance_criteria>
    - `apps/frontend/src/locales/de.json` contains `"common"`, `"auth"`, `"dashboard"`, `"upload"`, `"theme"` top-level keys
    - `apps/frontend/src/locales/en.json` contains the same 5 top-level keys
    - `apps/frontend/src/i18n.ts` contains `declare module "i18next"` with `CustomTypeOptions`
    - `apps/frontend/src/i18n.ts` contains `detectLocale` function reading `acm_lang` cookie
    - `apps/frontend/src/main.tsx` contains `import "./i18n.js"` before any component imports
    - `apps/frontend/src/main.tsx` contains `ThemeProvider` wrapping the tree
    - `pnpm -w typecheck` exits 0
  </acceptance_criteria>
  <done>i18next is initialized before React renders, translation files are feature-namespaced with complete key sets, typed keys prevent missing-key usage at compile time, ThemeProvider wraps the app.</done>
</task>

</tasks>

<verification>
- `grep -c "\-\-kpi-ok" apps/frontend/src/styles/global.css` returns 2
- `grep "prefers-color-scheme" apps/frontend/index.html` returns the inline FOUC script line
- `node -e "const de=require('./apps/frontend/src/locales/de.json'); const en=require('./apps/frontend/src/locales/en.json'); console.log(Object.keys(de).join(',') === Object.keys(en).join(',') ? 'key-parity-ok' : 'MISMATCH')"` — passes
- `pnpm -w typecheck` exits 0
</verification>

<success_criteria>
Dark mode CSS is verified for WCAG AA (4.5:1 body, 7:1 KPI values). i18next is initialized with typed resources, both locale files exist with matching key sets. ThemeProvider wraps the app. No FOUC on first load.
</success_criteria>

<output>
After completion, create `.planning/phases/06-dark-light-mode-i18n/06-02-SUMMARY.md`
</output>
